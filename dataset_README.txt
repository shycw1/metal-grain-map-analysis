run dataset_generation.py to generate mass grain map data. However, due to size limit for submission, only 5 grain map data was included in the dataset folder.

The amount of data used in the project was 3000.